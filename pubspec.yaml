name: oneonones
description: One-on-one's
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.0
  flutter_mobx: ^2.0.0
  flutter_modular: ^3.0.3
  mobx: ^2.0.1
  shared_preferences: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.12.2
  mobx_codegen: ^2.0.1+3

flutter:
  uses-material-design: true
